[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "thetraitors"
authors = [{name = "Pedro Curvo", email = "contact@pedrocurvo.com"}]
dynamic = ["version", "description"]
